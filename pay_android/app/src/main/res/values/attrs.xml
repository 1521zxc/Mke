<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="CountDownProgress">

        <!-- 圆实心的颜色 -->
        <attr name="circSolidColor" format="color"></attr>

        <!-- 圆边框的颜色 -->
        <attr name="circFrameColor" format="color"></attr>

        <!-- 进度条的颜色 -->
        <attr name="progressColor" format="color"></attr>

        <!-- 文字的颜色 -->
        <attr name="textColor" format="color"></attr>

    </declare-styleable>


    <declare-styleable name="NumberPicker">
        <attr name="normalTextColor" format="color" />
        <attr name="normalTextSize" format="dimension" />
        <attr name="selecredTextColor" format="color" />
        <attr name="selecredTextSize" format="dimension" />
        <attr name="unitHight" format="dimension" />
        <attr name="itemNumber" format="integer"/>
        <attr name="lineColor" format="color" />
        <attr name="maskHight" format="dimension"/>
        <attr name="noEmpty" format="boolean"/>
        <attr name="isEnable" format="boolean"/>
    </declare-styleable>

    <declare-styleable name="HeadZoomLayout">
        <!--头部视图id-->
        <attr format="reference" name="headViewId" />
        <!--头部最大放大比例-->
        <attr format="float" name="maxZoomRatio" />
        <!--是否可以放大头部-->
        <attr format="boolean" name="zoomEnable" />
        <!--最大拖拽距离-->
        <attr format="float" name="maxDragDistance"/>
        <!--放大加速度比例-->
        <attr format="float" name="dragAccelerationRatio"/>
    </declare-styleable>
</resources>